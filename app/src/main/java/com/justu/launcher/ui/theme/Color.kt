package com.justu.launcher.ui.theme

import androidx.compose.ui.graphics.Color

// Zen Dark Palette (Inspired by Night/Midnight)
val PureBlack = Color(0xFF000000)
val DarkSurface = Color(0xFF0A0A0C)
val DarkGray = Color(0xFF161618)
val LightText = Color(0xFFF5F5F7)
val MutedText = Color(0xFF8E8E93)

// Zen Warm Light Palette (Inspired by Sunrise/Paper/Sand)
val WarmBackground = Color(0xFFFDF8F3) // Warm off-white/paper
val WarmSurface = Color(0xFFFFFFFF)
val WarmSand = Color(0xFFF7EFE5)
val WarmGray = Color(0xFF8B8178)

// Accents
val ZenAmber = Color(0xFFFFB300)   // Warm Amber
val ZenOrange = Color(0xFFFF8A65)  // Soft Orange/Peach
val ZenPeach = Color(0xFFFFCCBC)   // Light Peach
val ZenGold = Color(0xFFFFD54F)    // Soft Gold

// Typography Colors
val ZenTextDark = Color(0xFF2D2926) // Deep warm gray/black
val ZenTextMuted = Color(0xFF706B66) // Muted warm gray
val ZenTextLight = Color(0xFFFDF8F3) // Warm off-white
